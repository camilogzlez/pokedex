
<header align="center">
  <%# <%= button_to "Home", root_path, method: :get %>
</header>

<div class="result-box">
  <div align="center">
    <h1><% @pokemons_details.each do |pokemon| %></h1>
      <h1>Name: <%=link_to pokemon[:name], pokemon_path(pokemon[:id]) %></h1>
      <%= image_tag "#{pokemon[:sprites][:other][:home][:front_default]}" %>
      <h1>Type: <% pokemon[:types].each do |type|%></h1> 
      </h1><%= type[:type][:name] %></h1>
    <h1><% end %></h1>
    <h1>Weight: <%= pokemon[:weight] %></h1>
    <h1>Habilities: <% pokemon[:abilities].each do |ability|%></h1> 
      </h1><%= ability[:ability][:name] %></h1>
    <h1><% end %></h1>
    <h1><% end %></h1>
<%= will_paginate @pokemons %>
  </div>

</div>
